import { Providers } from '@/app/providers'
import { Layout } from '@/components/Layout'

import '@/styles/tailwind.css'

export const metadata = {
  title: {
    template: '%s - Tyler Sustare',
    default: 'Tyler Sustare - Software leader, lifelong learner, and coach',
  },
  description:
    'I’m Tyler, a software leader and coach based in Bend, Oregon.  As a side hustle I’m co-founder and CTO of Archery Guru , a startup that aims to help archers improve their skills and organize archery events.',
  alternates: {
    types: {
      'application/rss+xml': `${process.env.NEXT_PUBLIC_SITE_URL}/feed.xml`,
    },
  },
}

export default function RootLayout({ children }) {
  return (
    <html lang="en" className="h-full antialiased" suppressHydrationWarning>
      <body className="flex h-full bg-zinc-50 dark:bg-black">
        <Providers>
          <div className="flex w-full">
            <Layout>{children}</Layout>
          </div>
        </Providers>
      </body>
    </html>
  )
}
